name = "tracpost-blog-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Route: all requests hitting blogs.tracpost.com and custom domains
# pointing to it via Cloudflare Custom Hostnames
routes = [
  { pattern = "blogs.tracpost.com/*", zone_name = "tracpost.com" }
]
